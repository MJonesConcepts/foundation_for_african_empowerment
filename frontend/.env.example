# Copy this to .env.local and fill in your values.
# Never commit .env.local to source control.

# URL of the Express backend (used server-side and for rewrites)
NEXT_PUBLIC_API_URL=http://localhost:4000
